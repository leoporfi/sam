// tailwind.config.js

/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      // Añade la ruta a todos tus archivos de componentes
      "./src/interfaz_web/components/**/*.py",
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }